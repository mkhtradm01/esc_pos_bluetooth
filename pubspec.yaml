name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.2.9
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth
publish_to: none

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0
  esc_pos_utils: ^0.3.6
  # esc_pos_utils:
  # path: ../esc_pos_utils
  flutter_bluetooth_basic:
    git:
      url: git://github.com/mkhtradm01/flutter_bluetooth_basic

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
